n_runs: 1
initial_seed: 42

validation:
  name: "holdout"
  target_missing: [0.20]

models:
  - name: gain
    model_cfg_path: "../configs/model/gain.yaml"
    training_cfg_path: "../configs/train/gain_train.yaml"