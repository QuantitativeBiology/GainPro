n_runs: 1
initial_seed: 42

validation:
  name: "holdout"
  missing_levels: [0.10]

models:
  - name: protogain
    model_config_path: "../configs/model/protogain.yaml"
    training_config_path: "../configs/train/gain_train.yaml"