n_runs: 1 # number of runs for each missingness level
initial_seed: 42

validation_strategy:
  # - name: "hold-out"
  #   missingness_levels: [0.10, 0.20, 0.30, 0.40]
  - name: "group-k-fold"
    num_folds: 5

models:
  - name: protogain
    model_config: "../configs/model/protogain.yaml"
    train_config: "../configs/train/train.yaml"