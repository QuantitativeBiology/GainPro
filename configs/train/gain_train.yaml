num_epochs: 100
batch_size: 128

generator_optimizer:
  lr: 1.0e-5
  weight_decay: 0.5
generator_scheduler:
  step: 100
  gamma: 0.5

discriminator_optimizer:
  lr: 1.0e-5
  weight_decay: 0.5
discriminator_scheduler:
  step: 100
  gamma: 0.5

alpha: 10
hint_rate: 0.5